# ========================================
# Budget 2026 AI Platform - Dependencies
# ========================================

# ---------- Web Framework ----------
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# ---------- Configuration & Settings ----------
pydantic==2.6.1
pydantic-settings==2.1.0
python-dotenv==1.0.1

# ---------- PDF Processing ----------
PyMuPDF==1.23.26  # fitz - primary PDF extractor
pdfplumber==0.10.4  # fallback for complex layouts

# ---------- OCR Fallback (optional) ----------
pytesseract==0.3.10
opencv-python==4.9.0.80
Pillow==10.2.0

# ---------- Data Handling ----------
pandas==2.2.0
numpy==1.26.4

# ---------- Text Processing ----------
nltk==3.8.1
tqdm==4.66.2

# ---------- Embeddings & AI Models ----------
sentence-transformers==2.3.1
torch==2.2.0  # required by sentence-transformers
transformers==4.37.2  # HuggingFace models

# ---------- Vector Databases ----------
# Production: Supabase pgvector (PostgreSQL extension)
supabase==2.3.4  # Supabase Python client
vecs==0.4.3  # Supabase vector toolkit

# Development/Local: FAISS (in-memory/disk)
faiss-cpu==1.7.4

# Alternative: Pinecone (cloud - if needed)
# pinecone-client==3.0.3

# Alternative: ChromaDB (embedded)
# chromadb==0.4.24

# ---------- LLM API Clients ----------
# Hybrid approach: OpenAI for embeddings, Groq for LLM
openai==1.12.0  # for embeddings (text-embedding-3-small)
groq==0.4.2  # for LLM (llama-3.1-8b-instant)
# anthropic==0.18.1  # for Claude (optional)

# ---------- HTTP Clients ----------
requests==2.31.0
httpx==0.26.0

# ---------- Serialization ----------
orjson==3.9.15  # fast JSON

# ---------- Logging ----------
loguru==0.7.2  # alternative logger (keeping for compatibility)

# ---------- Utilities ----------
python-dateutil==2.8.2
typing-extensions==4.9.0

# ---------- Development Tools (optional) ----------
# pytest==8.0.0
# black==24.1.1
# ruff==0.2.1
